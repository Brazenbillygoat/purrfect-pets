  <img src= <%= "#{@pet.picture}" %> alt= "A picture of the animal." />
  <h1><%= "#{@pet.name} the #{@pet.species}" %></h1>

  <p> <%= @pet.age %>  </p>
  <p> <%= @pet.good_or_bad %> </p>
  <%= link_to("Back to index", pets_path) %>
